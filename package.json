{
  "name": "claude-ttl-counter",
  "displayName": "Claude TTL Counter",
  "description": "Claude Code prompt cache TTL countdown and cache visibility / Claude Code 프롬프트 캐시 TTL 카운트다운과 캐시 가시성 도구",
  "version": "0.1.0",
  "publisher": "purpleprintai",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/PurplePrintAI/save-ur-tokens-ttl-counter-for-claude-code"
  },
  "engines": {
    "vscode": "^1.99.0"
  },
  "categories": [
    "Other"
  ],
  "keywords": [
    "claude",
    "claude-code",
    "ttl",
    "prompt-cache",
    "cache",
    "token"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "claudeTtl.toggleMode",
        "title": "Claude TTL: Toggle cache mode"
      }
    ]
  },
  "scripts": {
    "compile": "tsc -p .",
    "build": "npm run compile",
    "watch": "tsc -watch -p ."
  },
  "devDependencies": {
    "@types/node": "^22.15.21",
    "@types/vscode": "^1.99.0",
    "typescript": "^5.8.3"
  }
}
